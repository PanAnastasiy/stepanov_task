# .github/workflows/ci_pipeline.yml
name: Monorepo CI Pipeline

on:
  push:
    branches:
      - main  # Или master, в зависимости от имени вашей основной ветки
  pull_request:
    branches:
      - main

jobs:
  lint_and_check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        run: pip install uv

      - name: Install dependencies (uv lock and sync)
        run: uv sync

      # --- 1. Python Linting/Formatting (Ruff/Black) ---
      - name: Run Ruff (Linting)
        run: uv run ruff check .

      - name: Run Black (Formatting Check)
        run: uv run black --check .

      # --- 2. SQL Linting (sqlfluff) ---
      - name: Run sqlfluff
        run: uv run sqlfluff lint --project-dir .

      # --- 3. dbt Connection Check (Using Secrets) ---
      - name: dbt Debug (Check Snowflake Connectivity)
        # Устанавливаем переменные окружения из секретов GitHub для dbt
        env:
          SNOWFLAKE_ACCOUNT: ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_ROLE: ${{ secrets.SNOWFLAKE_ROLE }}
          SNOWFLAKE_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
          SNOWFLAKE_DATABASE: ${{ secrets.SNOWFLAKE_DATABASE }}
          SNOWFLAKE_SCHEMA: ${{ secrets.SNOWFLAKE_SCHEMA }}
        run: |
          # Запускаем dbt debug из корня проекта, указывая путь к dbt-проекту
          uv run dbt debug --project-dir dbt_project